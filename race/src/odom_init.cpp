#include "odom_init.h"
#include "race/odom_init_srv.h"

void odom_init(float x,float y,float w)
{
    // ros::NodeHandle nh;
    // ros::ServiceClient client = nh.serviceClient<race::>("init");
    // race::odom_init_srv srv;

    // if (client.call(srv))
    // {
    //     ROS_INFO("Sum: %ld", (long int)srv.response.sum);
    // }

}